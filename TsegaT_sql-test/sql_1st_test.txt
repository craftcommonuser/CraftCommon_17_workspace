Short Answer


      What is Data? data is unprocessed information, It can be recorded in different ways. 


2.       What is Information? 
        when we record any unprocessed data that is information. 

3.      What is Database(DB)?
         The systematic collection of related data.
          DB is a container for related data. 
 
4.      What is the Relation Database Management System(RDBMS)? 
            It is a software and it helps us to manage the related data.

5.      Define the importance of Relation Database Management System(RDBMS)?
            Make it easy to manage large amounts of information, handle security,  backups, concurrency and interact with system applications.
       
6.     As we all know that there are Two types of Database. Relational Database(SQL) AND Non-Relational DB(NO sql). what is the difference between them.
Relational DB: only for related data and stored in the form of a table.
Non DB: store as a form of document and unstructured. more flexible


7. 	List examples of Relation Database Management System(RDBMS)?
                mysql, oracle, IBM, microsoft…

8.       List examples of Non-Relational DB(Nosql)? 
mangoDB, cassandra

9.       Define and Describe is Structured Query Language(SQL)?
     The core of a relational DB which is used for accessing and managing the DB.
     SQL is standard language to interact with RDBM and mainly perform create, read, update, and delete

10.       List and Describe each of the different subsets of SQL(Mean DDL, DML, DCL, TCL)?
DDL(data definition language):it make change in to structure of the table by create, alter, truncate, drop and rename) and it is auto-committed 
 DML(data manipulation language) : used for manipulating the data stored in the table. By insert, update, delete, and select.
DCL(data control language) grants or revokes data accesses permitions.
TCL(transaction control language) used to manage the data transaction by DML statement. we can commit, rollback, savepoint, and set transactions.

11.      what is table in Database(DB)?
table is a typical model after a real world entity and it contains row and column.

12.     what is column and Row(tuples) in table?
column is represent the attribute of entity. 
Row consists of a set of values for one instance of an entity.
